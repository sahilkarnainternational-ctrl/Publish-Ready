{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "vercel-build": "pnpm run build:frontend",
    "build:frontend": "cd artifacts/axyomis && pnpm exec vite build --config ../../vite.vercel.config.ts",
    "dev": "node scripts/serve-with-proxy.js",
    "dev:frontend": "pnpm --filter @workspace/axyomis run dev",
    "dev:api": "PORT=8080 pnpm --filter @workspace/api-server run dev"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "express": "^5.2.1",
    "http-proxy-middleware": "^4.0.0"
  }
}
